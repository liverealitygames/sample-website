services:
  lrg-local:
    container_name: lrg
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src/lrg:/app
    env_file:
      - sample-docker-compose.env
    ports:
      - 8080:8080
    entrypoint: ["scripts/startup.sh"]